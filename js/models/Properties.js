/**
 * Properties is a Backbone model representing the properties of a GeoJSON Feature.
 *
 */
define([
    'backbone'
], function (Backbone) {
    var Properties = Backbone.Model.extend({
    });
    return Properties;
});
