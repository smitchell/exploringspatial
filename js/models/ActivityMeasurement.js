/**
 * ActivityMeasurement is a Backbone model representing a Record from a Garmin FIT file.
 *
 */
define([
    'underscore',
    'backbone'
], function (_, Backbone) {
    var ActivityMeasurement = Backbone.Model.extend({
    });
    return ActivityMeasurement;
});
