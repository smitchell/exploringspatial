/**
 * Activity is a Backbone model representing a Garmin Activity.
 *
 */
define([
    'backbone'
], function (Backbone) {
    var Geometry = Backbone.Model.extend({
    });
    return Geometry;
});
