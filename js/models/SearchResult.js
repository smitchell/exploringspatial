/**
 * SearchResult is a Backbone model representing a single search result.
 */
define([
        'backbone'
], function(Backbone) {
  var SearchResult = Backbone.Model.extend({
  });

  return SearchResult;
});
