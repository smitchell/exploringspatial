/**
 * RssEntry is a Backbone model representing a single entry in an RSS feed.
 */
define([
        'backbone'
], function(Backbone) {
  var RssEntry = Backbone.Model.extend({
  });

  return RssEntry;
});
